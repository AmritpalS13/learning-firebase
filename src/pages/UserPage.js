import React, { useState } from 'react';


//Will display only the user's list's that is one that is unique to them and the UID's
const Userpage = () => {
    return (
        <div>

        </div>
    )
}

export default UserPage;